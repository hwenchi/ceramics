# Remote Desktop

An XFCE desktop with Firefox installed, accessible from a browser tab.

Firefox is not opened automatically. It can be launched from the XFCE application menu, or by running `firefox-esr` in a terminal, once the desktop loads.
