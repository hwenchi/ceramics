# Stream Filter

Filtered video and audio for a YouTube link, muxed into a live WebM and played directly in a `<video>` tag.

A default video is used on start. The page has a box to paste in a different YouTube URL, and dropdowns to switch the video and audio filter, all without restarting.
